# == Schema Information
#
# Table name: roles
#
#  id    :integer(4)      not null, primary key
#  name  :string(255)
#  state :boolean(1)      default(TRUE)
#

admin_role:
  name: admin
user_role:
  name: user
