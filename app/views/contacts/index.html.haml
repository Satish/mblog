#contactsListing
  %p= params[:query].blank? ? "Search User" : "Searching for: " + h(params[:query])
  = render :partial => 'shared/search'
  %p{ :style => "font-size:11px;" } (Search by: login, email address or name)

  - if @contacts.empty?
    = params[:query].blank? ? "No contacts found" : ("No results found for: " + h(params[:query]))
  - else
    = render @contacts
    .pagination
      = will_paginate @contacts