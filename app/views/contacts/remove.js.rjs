if current_user.destroy_contact_following(@user)
  page.replace "followUser_#{ @user.login }", :partial => "shared/follow_link", :locals => { :user => @user }
  page.replace_html "#{ @user.login }s_followers", pluralize(@user.reload.followers_count, 'Follower')
else
  page.render :nothing => true
end
